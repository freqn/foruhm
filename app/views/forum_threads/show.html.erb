<div class="container">
  <h1><%= @forum_thread.subject %></h1>
  <p class="text-muted">Author: <%= @forum_thread.user.name %> - <%= local_time(@forum_thread.created_at, '%B %e, %Y at %l:%M%P')%> </p>
  <%= render @forum_thread.forum_posts %>

  <hr />
  <%= render partial: "forum_posts/form" %>
</div>